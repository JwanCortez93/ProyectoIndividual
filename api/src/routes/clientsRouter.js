// TODO const { Router } = require("express");
// const {
//   getClientHandler,
//   getClientsHandler,
//   deleteClientHanlder,
// } = require("../handlers/clientsHandlers");

// const clientsRouter = Router();

// clientsRouter.get("/", getClientsHandler);
// clientsRouter.get("/:id", getClientHandler);
// clientsRouter.delete("/:id", deleteClientHanlder);

// module.exports = clientsRouter;
